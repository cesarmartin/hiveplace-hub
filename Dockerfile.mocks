FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install

COPY tsconfig.json ./
COPY mocks ./mocks

# Each mock container chooses which file to run via CMD override in compose
CMD ["npx", "ts-node", "mocks/pluggy-mock.ts"]
